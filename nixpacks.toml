# Ensures Nixpacks/Railpack uses pnpm for this monorepo (not npm).
# Per-service build/start commands live in apps/*/railway.toml.

[phases.setup]
nixPkgs = ["nodejs_20", "pnpm-9_x"]

[variables]
NIXPACKS_NODE_VERSION = "20"
