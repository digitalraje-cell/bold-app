import { MaxPlanPageContent } from '@/components/marketing/MaxPlanPageContent';

export const metadata = {
  title: 'Max — Coming Soon | Bold',
  description:
    'Bold Max for creators, coaches, agencies and broadcasters. Multi-platform live streaming — launching soon.',
};

export default function MaxPlanPage() {
  return <MaxPlanPageContent />;
}
