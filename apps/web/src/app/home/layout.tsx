export const metadata = {
  title: 'Join Meeting — Bold',
  description: 'Join a Bold video meeting with a meeting ID or link.',
};

export default function PwaHomeLayout({ children }: { children: React.ReactNode }) {
  return children;
}
